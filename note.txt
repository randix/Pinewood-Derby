issues
  resume by default (must enter pin)
  
  (fixed?)crash with not all tracks
  search for server, or: at least cache it.
  (fixed?)averages when not included time - recalculate after ignoring or unignoring.
  
Server:
  server: add cert for https

RESTView:
  change to https

TestFlight:
    add background photo
    everything for the app store

consider - supporting 8 tracks (major mods to all views, or force landscape only usage on phone, or only on iPad...)
